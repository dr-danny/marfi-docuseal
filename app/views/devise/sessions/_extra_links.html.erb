<% unless HexclavePilot::Config.available? %>
  <p class="text-center mt-4"><%= link_to 'Use passwordless sign-in', hexclave_pilot_path, class: 'link link-hover', data: { turbo: false } %></p>
<% end %>

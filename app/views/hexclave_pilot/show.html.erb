<%= content_for(:canonical_url, hexclave_pilot_url) %>
<div class="max-w-lg mx-auto px-2">
  <h1 class="text-4xl font-bold text-center mt-8">Sign in</h1>
  <p class="text-center mt-3">Use your approved <span class="font-semibold">@marfi.io</span> identity.</p>
  <div class="mt-8">
    <%= render 'hexclave_pilot/form' %>
  </div>
  <p class="text-center mt-6"><%= link_to 'Use native sign-in', new_user_session_path, class: 'link link-hover' %></p>
</div>

<turbo-frame id="modal">
  <turbo-modal class="modal modal-open items-start !animate-none overflow-y-auto marfi-modal" data-close-after-submit="<%= local_assigns.key?(:close_after_submit) ? local_assigns[:close_after_submit] : true %>">
    <div class="absolute top-0 bottom-0 right-0 left-0 marfi-modal-backdrop" data-action="click:turbo-modal#close"></div>
    <div class="modal-box marfi-modal-box pt-4 pb-6 px-6 mt-20 max-h-none">
      <% if local_assigns[:title] %>
        <div class="marfi-modal-header flex justify-between items-center border-b pb-2 mb-2 font-medium">
          <span class="marfi-modal-title">
            <%= local_assigns[:title] %>
          </span>
          <a href="#" class="marfi-modal-close text-xl" data-action="click:turbo-modal#close" aria-label="Close">&times;</a>
        </div>
      <% end %>
      <div>
        <%= yield %>
      </div>
    </div>
  </turbo-modal>
</turbo-frame>
